# Deny all by default
Order Deny,Allow
Deny from all

# Allow access to book files only
<FilesMatch "\.(pdf|epub)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>

# Prevent PHP execution
<FilesMatch "\.php$">
    Order Deny,Allow
    Deny from all
</FilesMatch>
